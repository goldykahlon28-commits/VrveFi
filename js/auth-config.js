/* ============================================================
   VrveFi — Auth configuration
   To enable "Continue with Google", create an OAuth 2.0 Client ID
   (type: Web) in Google Cloud Console, add your site to "Authorized
   JavaScript origins" (e.g. https://vrvefi.onrender.com and
   http://localhost:4173), then paste the Client ID below.
   Leave it blank to hide the Google button and use email sign-in only.
   ============================================================ */
window.VRVEFI_GOOGLE_CLIENT_ID = '';
